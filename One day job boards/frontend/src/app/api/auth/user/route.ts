// This file is kept for reference but all auth is handled by the FastAPI backend
// The API client in @/lib/api.ts handles all authentication calls

export async function GET() {
  throw new Error('Use the api client in @/lib/api.ts for authentication');
}
